[Feynman: BaseLSTAccumulator] This contract accepts WETH, usually turns it into stETH, and tells the Yearn accounting layer how much WETH-equivalent value it thinks the strategy owns. Users can only withdraw loose WETH. Roles are expected to move stETH back into WETH or through the Lido queue when liquidity is needed.
[Feynman: availableWithdrawLimit] This answers one plain question: how much WETH is sitting in the strategy right now and can be paid out immediately. It does not ask how much stETH, wstETH, vault shares, or queued Lido value exists.
[Inversion: availableWithdrawLimit] 1. Create WETH by unwinding stETH at a loss, then let the fastest shareholder withdraw using the old share price. 2. Claim or manually free queued value in a way that changes WETH liquidity before the next report. 3. Leave deposits open so a favored address can enter after a role-created accounting loss.
[Feynman: manualSwapToAsset] Management chooses how much stETH-side value to turn back into WETH and how little WETH it is willing to accept. The function creates user-withdrawable WETH but does not update the Yearn total-assets number.
[Socratic: BaseLSTAccumulator.manualSwapToAsset — why?] Why is it safe to expose newly received WETH to withdrawals before the next report if the swap accepted less WETH than the last reported stETH value?
[Feynman: _emergencyWithdraw] During shutdown, an emergency role asks the strategy to pull value back from stETH into WETH so users can exit. In this implementation the Curve minimum is zero, so the role can create liquid WETH even when the exit price is below the last reported value.
[Inversion: _emergencyWithdraw] 1. Shutdown, swap stETH to WETH at a discount, then let one holder redeem before losses are reported. 2. Swap only part of the position, leaving enough idle WETH for a favored partial exit. 3. Use a stressed Curve pool so the no-minimum emergency path realizes a larger discount than the current share price reflects.
[Feynman: TokenizedStrategy._withdraw] This burns a user's shares, subtracts the requested amount from the stored accounting total, and transfers WETH. If there is already enough loose WETH, it does not ask the strategy to revalue the remaining position first.
[Inversion: TokenizedStrategy._withdraw] 1. Make loose WETH available after an off-book loss and redeem before report. 2. Use `redeem` rather than `withdraw` so the holder takes the maximum allowed by idle WETH. 3. Leave the loss with holders who do not exit in the same window.
[Feynman: estimatedTotalAssets] This adds loose WETH plus the reported stETH-side value after applying a management-chosen haircut.
[Socratic: BaseLSTAccumulator.estimatedTotalAssets — why?] What stops the haircut from being used as a temporary share-price lever rather than a conservative valuation input?
[Feynman: setReportBuffer] Management can change the haircut used for reports. The setter does not bound the value to basis points.
[Inversion: setReportBuffer] 1. Set the haircut near 100%, skip health check, report a large loss, then let a favored depositor mint cheaply. 2. Reset the haircut, skip health check again, and report the value back as profit. 3. Set the haircut above 100% to make total-assets reads and reports revert.
[Feynman: initiateLSTWithdrawal] Management moves stETH into the Lido withdrawal queue and records only one aggregate pending number.
[Feynman: claimLSTWithdrawal] A keeper or management supplies claim data, the strategy claims one queue request, and the aggregate pending number is reduced by the ETH actually received.
[Socratic: BaseLSTAccumulator.claimLSTWithdrawal — why?] Why is a single caller-supplied request ID enough to update a global pending number when the contract does not store request IDs or request amounts?
[Feynman: manualClaimWithdrawals] An emergency role can batch-claim queue requests and optionally set the entire pending counter to zero.
[Inversion: manualClaimWithdrawals] 1. Claim only part of the queued value with the zero flag set. 2. Use an old request after a prior manual clear to affect a newer pending counter. 3. Clear pending first, report a loss, then later claim the omitted request as profit.
[Feynman: Strategy4626Factory.newStrategy4626] Anyone can create the one factory-recorded strategy for a chosen wstETH vault. The factory stamps its current role defaults onto the new strategy and leaves the new management address pending acceptance.
[Inversion: Strategy4626Factory.newStrategy4626] 1. Deploy for a vault just before role defaults rotate so the new strategy snapshots stale defaults. 2. Deploy for an arbitrary wstETH ERC4626 vault and rely on off-chain consumers treating the factory event as canonical. 3. Permanently occupy the one deployment slot for a vault because there is no reset path.
[Feynman: Strategy4626] This extension wraps stETH into wstETH and deposits it into another vault. It treats that external vault's conversion, deposit capacity, and redemption answers as the source of truth for strategy value and liquidity.
[Socratic: Strategy4626.constructor — why?] Why is checking only that the vault asset is wstETH enough when later accounting trusts multiple vault methods that can be non-standard or adversarial?

FINDING | contract: BaseLSTAccumulator | function: manualSwapToAsset/_emergencyWithdraw | bug_class: stale-accounting-loss-shift | group_key: BaseLSTAccumulator | manualSwapToAsset/_emergencyWithdraw | stale-accounting-loss-shift
path: management/emergency role -> stETH-to-WETH unwind -> idle WETH increases while TokenizedStrategy totalAssets remains at the last report -> first redeemer exits at stale PPS -> remaining holders absorb the unwind loss
seam: access×economics×asymmetry
actor: any shareholder able to redeem after a role-triggered lossy unwind and before the next report; a role holder can favor itself or a chosen holder by timing the unwind and withdrawal
proof: Start with 100 WETH reported totalAssets, 100 shares outstanding, no idle WETH, and two users with 50 shares each. Management calls `manualSwapToAsset(100e18, 90e18)` during a 10% stETH/Curve discount, or emergency calls `emergencyWithdraw` after shutdown, which reaches `_swapLSTToAsset` with a zero minimum. The strategy now holds 90 WETH, but TokenizedStrategy still stores 100 totalAssets because only `report()` assigns `S.totalAssets = newTotalAssets`. `availableWithdrawLimit()` returns the 90 WETH idle balance. User A redeems 50 shares before the next report: `_convertToAssets(50)` uses stale 100 totalAssets / 100 supply and returns 50 WETH, `maxRedeem` allows it because 90 WETH is idle, and `_withdraw` transfers 50 WETH. A fair post-unwind pro-rata amount was 45 WETH, so User A extracts 5 WETH from User B. After the withdrawal, only 40 WETH remains for User B's 50 shares and the loss is pushed to the non-redeemer.
description: Role-controlled manual liquidity creation can expose WETH to ERC4626 withdrawals before the accounting layer records the unwind loss, letting early redeemers exit at stale value.
fix: After any manual or emergency unwind that changes liquid WETH, either force a successful report before withdrawals can use the new idle balance, or set a stale-accounting flag that makes `availableWithdrawLimit` zero until the loss/profit is reported.

LEAD | contract: BaseLSTAccumulator/Strategy | function: claimLSTWithdrawal/manualClaimWithdrawals | bug_class: unbound-queue-claim-accounting | group_key: BaseLSTAccumulator | claimLSTWithdrawal/manualClaimWithdrawals | unbound-queue-claim-accounting
code_smells: `initiateLSTWithdrawal` records only aggregate `pendingRedemptions`, while `claimLSTWithdrawal` accepts arbitrary keeper-supplied bytes and `Strategy._claimLSTWithdrawal` decodes a single request ID; pending is decreased by ETH received, not by a stored request ID/amount pair. The initiate path returns `abi.encode(uint256[] requestIds)`, but the tested claim path decodes that array and re-encodes `requestIds[0]`, confirming the stored return data is not itself bound to the later claim. `manualClaimWithdrawals` can also set `pendingRedemptions = 0` independent of the amounts actually claimed.
description: The Lido queue handoff can desynchronize the scalar pending value from real queue requests if old, partially cleared, or manually claimed request IDs exist; I did not promote this to a finding because proving an untrusted exploit depends on a reachable untracked strategy-owned request in the real queue lifecycle.

LEAD | contract: BaseLSTAccumulator | function: setReportBuffer | bug_class: role-controlled-nav-haircut | group_key: BaseLSTAccumulator | setReportBuffer | role-controlled-nav-haircut
code_smells: `setReportBuffer` has no `<= MAX_BPS` bound even though `estimatedTotalAssets` computes `MAX_BPS - reportBuffer`. Values above 100% revert valuation paths, and values near 100% let management manufacture a report loss; paired with `setDoHealthCheck(false)`, `setOpenDeposits`/`setAllowed`, and a later reset, this becomes a privileged cheap-mint/profit-report value shift. Factory-created Strategy4626 instances also set `profitMaxUnlockTime(0)`, making restored profits immediately reflected in PPS.
description: This is a concrete management trust lever over share pricing, but it depends on management intentionally combining health-check bypass and deposit gating, so I leave it as a trust-model lead rather than an untrusted exploit finding.

LEAD | contract: Strategy4626Factory | function: newStrategy4626 | bug_class: permissionless-role-snapshot | group_key: Strategy4626Factory | newStrategy4626 | permissionless-role-snapshot
code_smells: Any caller can create the single factory deployment for a `_vault`; the strategy snapshots current `keeper`, `emergencyAdmin`, `performanceFeeRecipient`, and pending `management`, then `deployments[_vault]` has no reset path. Tests confirm the new strategy's active management is the factory until the pending management accepts, and defaults such as `profitMaxUnlockTime(0)` are stamped at deployment time.
description: A factory caller can permanently bind a vault to the factory's current defaults or stale pre-rotation roles, but user impact requires management acceptance or off-chain consumers treating the permissionless event as sufficient endorsement.

LEAD | contract: Strategy4626 | function: constructor/valueOfWstETH/_freeStETH | bug_class: downstream-erc4626-trust-boundary | group_key: Strategy4626 | constructor/valueOfWstETH/_freeStETH | downstream-erc4626-trust-boundary
code_smells: The only vault identity check is `vault.asset() == wstETH`, while later accounting and liquidity trust `maxDeposit`, `convertToAssets`, `previewWithdraw`, `maxRedeem`, `deposit`, and `redeem`. Through the permissionless factory, an arbitrary wstETH ERC4626 vault can become a recorded deployment even if its conversion or redemption behavior is adversarial or caller-sensitive.
description: A malicious or non-standard downstream vault can distort deposit limits, reported value, or unwind sizing, but a concrete loss path requires management to accept/open the strategy or users to rely on the factory record before management endorsement.
